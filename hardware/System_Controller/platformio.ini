; [env:esp32-s3-devkitc-1]
; ;platform = espressif32 @ 6.10.0
; platform = https://github.com/pioarduino/platform-espressif32/releases/download/53.03.11/platform-espressif32.zip
; board = esp32-s3-devkitc-1
; framework = arduino
; monitor_speed = 115200

; ; Налаштування пам'яті для плат N16R8
; board_build.arduino.memory_type = qio_opi
; board_build.flash_size = 16MB
; board_build.partitions = default_16MB.csv

; ; Прапорці збірки для активації USB-CDC та апаратної PSRAM
; build_flags =
;     -D ARDUINO_USB_CDC_ON_BOOT=1
;     -D ARDUINO_USB_MODE=1
;     -D BOARD_HAS_PSRAM
;     -mfix-esp32-psram-cache-issue

; lib_deps =
;     me-no-dev/ESPAsyncWebServer @ ^1.2.4
;     me-no-dev/AsyncTCP @ ^1.1.1
;     bblanchon/ArduinoJson @ ^7.0.4
[env:esp32-s3-devkitc-1]
; Твій перевірений лінк на свіже стабільне ядро 3.х
platform = https://github.com/pioarduino/platform-espressif32/releases/download/53.03.11/platform-espressif32.zip
board = esp32-s3-devkitc-1
framework = arduino
monitor_speed = 115200

; Налаштування пам'яті для плат N16R8
board_build.arduino.memory_type = qio_opi
board_build.flash_size = 16MB
board_build.partitions = default_16MB.csv

build_flags =
    -D ARDUINO_USB_CDC_ON_BOOT=1
    -D ARDUINO_USB_MODE=1
    -D BOARD_HAS_PSRAM
    -mfix-esp32-psram-cache-issue
    -I include

lib_deps =
    ; ФІКС: Використовуємо сучасні форки, які вміють працювати всередині задач FreeRTOS на ядрі 3.х
    mathieucarbou/ESPAsyncWebServer @ ^3.6.0
    mathieucarbou/AsyncTCP @ ^3.3.2
    bblanchon/ArduinoJson @ ^7.0.4
    adafruit/Adafruit NeoPixel @ ^1.12.0
    adafruit/Adafruit PWM Servo Driver Library @ ^3.0.2
